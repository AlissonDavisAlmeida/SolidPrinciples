export class VehicleHelper {
  name: string;
  model: string;
  year: number;
  price: number;

  constructor(name: string, model: string, year: number, price: number) {
    this.name = name;
    this.model = model;
    this.year = year;
    this.price = price;
  }
}
